# Mandatory fields
sonar.projectKey=ansible.project
sonar.projectName=Ansible Common conifg
sonar.projectVersion=1.0
sonar.qualitygate.wait=true

# Comma-separated paths to directories with sources (required)
sonar.sources=.
