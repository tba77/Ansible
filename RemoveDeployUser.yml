---
- name: Removing user Deploy after finishing deployment
  hosts: "{{ host_name }}"
  remote_user: "{{ remote_user }}"
  become: yes
  become_method: "sudo"

  tasks:
    - name: Remove User Deploy
      user:
          name: deploy
          state: absent
          remove: yes
